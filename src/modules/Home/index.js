import React from 'react';

const Home = () => (
    <div>Homes</div>
);

const routes = {
    routeProps: {
        path: '/',
        component: Home,
    },
    name: 'home',
}

export default routes;

